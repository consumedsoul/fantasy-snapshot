# Weekly Audit - 2026-02-09

**Auditor:** Claude Code
**Duration:** 15 minutes
**Files Scanned:** 5
**Last Audit:** Initial audit

## Summary

- 🔴 Critical Issues: 2
- 🟠 High Priority: 8
- 🟡 Medium Priority: 12
- ⚪ Low Priority: 7

**Total Action Items:** 29

---

## 🔴 Critical Issues (Fix Immediately)

### Hardcoded Email Address in Production Code
**File:** `Code.gs`
**Lines:** 1184
**Risk:** Anyone with access to the script can see the personal email address. If the script is accidentally shared or the code is exposed, this email will receive snapshots meant for someone else.

**Current Code:**
```javascript
MailApp.sendEmail('hun@ghkim.com', subject, output);
```

**Recommended Fix:**
```javascript
var recipientEmail = PropertiesService.getScriptProperties().getProperty('RECIPIENT_EMAIL');
if (!recipientEmail) {
  throw new Error('Missing RECIPIENT_EMAIL script property.');
}
MailApp.sendEmail(recipientEmail, subject, output);
```

**Why:** Secrets and configuration (including personal email addresses) should never be hardcoded. This makes the code portable and prevents accidental exposure.

---

### No Error Handling in Main Entry Point
**File:** `Code.gs`
**Lines:** 1155-1185
**Risk:** If `pullFantasyData()` fails (API error, auth failure, email quota exceeded), the user gets no notification and the script fails silently. This is especially critical if run on a trigger.

**Current Code:**
```javascript
function pullFantasyData() {
  var leagues = getAllLeagues_();
  if (!leagues || !leagues.length) {
    Logger.log('No leagues found for this Yahoo account.');
    return;
  }
  // ... rest of function with no try-catch
}
```

**Recommended Fix:**
```javascript
function pullFantasyData() {
  try {
    var leagues = getAllLeagues_();
    if (!leagues || !leagues.length) {
      Logger.log('No leagues found for this Yahoo account.');
      MailApp.sendEmail(getRecipientEmail_(), 'Fantasy Snapshot Error', 'No leagues found for this Yahoo account.');
      return;
    }

    var sections = [];
    var completedWeek = null;

    for (var i = 0; i < leagues.length; i++) {
      var snapshot = buildLeagueSnapshot_(leagues[i]);
      sections.push(snapshot);

      if (completedWeek === null) {
        var cw = getCurrentWeek_(leagues[i].league_key);
        completedWeek = Math.max(1, cw - 1);
      }
    }

    var output = sections.join('\n\n');
    Logger.log(output);

    var subject = 'Yahoo Fantasy Snapshot';
    if (completedWeek !== null) {
      subject += ' - Week ' + completedWeek;
    }

    MailApp.sendEmail(getRecipientEmail_(), subject, output);

  } catch (err) {
    Logger.log('Critical error in pullFantasyData: ' + err.message);
    Logger.log('Stack trace: ' + err.stack);

    try {
      MailApp.sendEmail(
        getRecipientEmail_(),
        'Fantasy Snapshot Error',
        'Failed to generate fantasy snapshot.\n\nError: ' + err.message + '\n\nCheck the Apps Script logs for details.'
      );
    } catch (mailErr) {
      Logger.log('Failed to send error notification email: ' + mailErr.message);
    }
  }
}

function getRecipientEmail_() {
  var email = PropertiesService.getScriptProperties().getProperty('RECIPIENT_EMAIL');
  if (!email) {
    throw new Error('Missing RECIPIENT_EMAIL script property.');
  }
  return email;
}
```

**Why:** Silent failures in scheduled jobs are dangerous. Users should always be notified when automation fails, especially via email since that's the expected output channel.

---

## 🟠 High Priority (This Week)

### No Rate Limiting Protection
**File:** `Code.gs`
**Lines:** 436-438, 731-733
**Risk:** Batched API calls in tight loops can exceed Yahoo's rate limits (unclear limits, but typically ~5-10 req/sec). This will cause 429 errors and script failures.

**Current Code:**
```javascript
for (var i = 0; i < playerKeys.length; i += chunkSize) {
  var chunk = playerKeys.slice(i, i + chunkSize);
  var path = '/league/' + leagueKey + '/players;player_keys=' + chunk.join(',') + '/stats;type=week;week=' + week;
  var data = yahooApiRequest_(path, {});
  // No delay between requests
}
```

**Recommended Fix:**
```javascript
for (var i = 0; i < playerKeys.length; i += chunkSize) {
  var chunk = playerKeys.slice(i, i + chunkSize);
  var path = '/league/' + leagueKey + '/players;player_keys=' + chunk.join(',') + '/stats;type=week;week=' + week;
  var data = yahooApiRequest_(path, {});

  // Add 200ms delay between batches to respect rate limits
  if (i + chunkSize < playerKeys.length) {
    Utilities.sleep(200);
  }
}
```

**Why:** Yahoo's rate limits aren't well documented, but conservative delays prevent 429 errors and script crashes.

---

### No Retry Logic for Transient Failures
**File:** `Code.gs`
**Lines:** 1298-1313, 33
**Risk:** Network blips, temporary API unavailability, or transient 500 errors will cause the entire script to fail. No retry = brittle automation.

**Recommended Fix:**
Add exponential backoff retry helper:
```javascript
function retryWithBackoff_(fn, maxRetries) {
  maxRetries = maxRetries || 3;
  var retries = 0;

  while (retries < maxRetries) {
    try {
      return fn();
    } catch (err) {
      retries++;
      if (retries >= maxRetries) {
        throw err;
      }

      var backoffMs = Math.pow(2, retries) * 1000; // 2s, 4s, 8s
      Logger.log('Retry ' + retries + ' after error: ' + err.message + '. Waiting ' + backoffMs + 'ms...');
      Utilities.sleep(backoffMs);
    }
  }
}
```

Then wrap `UrlFetchApp.fetch()` calls:
```javascript
var response = retryWithBackoff_(function() {
  return UrlFetchApp.fetch(url, options);
});
```

**Why:** Transient failures are common in distributed systems. Retry logic makes automation resilient.

---

### Missing Input Validation
**File:** `Code.gs`
**Lines:** 428, 721, 887, 494, 643, 815
**Risk:** If callers pass invalid `week` values (negative, zero, string, null), functions will make bad API calls or crash.

**Current Code:**
```javascript
function getWeekPointsMapForPlayerKeys_(week, playerKeys, leagueKey) {
  // No validation of week parameter
  var path = '/league/' + leagueKey + '/players;player_keys=' + chunk.join(',') + '/stats;type=week;week=' + week;
}
```

**Recommended Fix:**
```javascript
function getWeekPointsMapForPlayerKeys_(week, playerKeys, leagueKey) {
  if (!week || week < 1 || week > 18) {
    throw new Error('Invalid week parameter: ' + week + '. Must be 1-18.');
  }
  leagueKey = leagueKey || getLeagueKey_();
  var map = {};
  // ...
}
```

**Why:** Fail fast with clear error messages. Invalid inputs should never make it to external APIs.

---

### Token Expiration Not Proactively Checked
**File:** `Code.gs`
**Lines:** 1273-1280
**Risk:** Script only checks token expiration after a 401 error. This wastes an API call and time on every first request after expiration.

**Current Code:**
```javascript
function getYahooAccessToken_() {
  var props = PropertiesService.getScriptProperties();
  var accessToken = props.getProperty('YAHOO_ACCESS_TOKEN');
  if (!accessToken) {
    throw new Error('No Yahoo access token stored. Run startYahooAuth() and complete the OAuth flow.');
  }
  return accessToken;
}
```

**Recommended Fix:**
```javascript
function getYahooAccessToken_() {
  var props = PropertiesService.getScriptProperties();
  var accessToken = props.getProperty('YAHOO_ACCESS_TOKEN');
  if (!accessToken) {
    throw new Error('No Yahoo access token stored. Run startYahooAuth() and complete the OAuth flow.');
  }

  // Check if token is expired based on stored metadata
  var expiresIn = parseInt(props.getProperty('YAHOO_EXPIRES_IN') || '0', 10);
  var createdAt = parseInt(props.getProperty('YAHOO_TOKEN_CREATED_AT') || '0', 10);

  if (expiresIn > 0 && createdAt > 0) {
    var expiresAt = createdAt + (expiresIn * 1000);
    var now = Date.now();
    var buffer = 5 * 60 * 1000; // 5 minute buffer

    if (now >= (expiresAt - buffer)) {
      Logger.log('Access token expired or expiring soon. Refreshing proactively.');
      refreshYahooAccessToken_();
      accessToken = props.getProperty('YAHOO_ACCESS_TOKEN');
    }
  }

  return accessToken;
}
```

**Why:** Proactive token refresh prevents wasted API calls and improves reliability.

---

### No Logging of API Call Count
**File:** `Code.gs`
**Lines:** All API request functions
**Risk:** No visibility into how many API calls are made per run. This makes it impossible to optimize for quota limits or diagnose slow runs.

**Recommended Fix:**
Add a counter and log at the end:
```javascript
var API_CALL_COUNT = 0;

function yahooApiRequest_(resourcePath, queryParams) {
  API_CALL_COUNT++;
  // ... existing logic
}

function pullFantasyData() {
  API_CALL_COUNT = 0; // Reset counter
  try {
    // ... existing logic

    Logger.log('Fantasy snapshot generated successfully. Total Yahoo API calls: ' + API_CALL_COUNT);

  } catch (err) {
    Logger.log('Error after ' + API_CALL_COUNT + ' Yahoo API calls: ' + err.message);
    // ...
  }
}
```

**Why:** Observability is critical for optimization and debugging. Knowing call counts helps identify bottlenecks.

---

### Missing Index Documentation
**File:** `CLAUDE.md`
**Lines:** N/A
**Risk:** The codebase lacks a function index. New developers (or AIs) must read 1377 lines to understand what functions exist.

**Recommended Fix:**
Add to CLAUDE.md:
```markdown
## Function Index

### Public Entry Points
- `pullFantasyData()` — Main entry point: fetches all leagues, builds snapshots, emails result
- `startYahooAuth()` — Initiates Yahoo OAuth flow (run once in IDE)
- `doGet(e)` — OAuth callback handler (Web App endpoint)
- `debugAllLeaguesRaw()` — Debug helper: logs raw Yahoo API response

### Private Helpers (suffix: `_`)

**Config & Auth:**
- `getConfig_()` — Reads script properties (credentials)
- `getLeagueKey_()` — Retrieves default league key from properties
- `getYahooAuthUrl_()` — Constructs Yahoo OAuth URL
- `getYahooAccessToken_()` — Retrieves access token, checks expiration
- `refreshYahooAccessToken_()` — Refreshes expired token

**API Transport:**
- `yahooApiRequest_(resourcePath, queryParams)` — Generic Yahoo API wrapper with auto-refresh
- `supabaseRequest_(path, method, payload)` — Generic Supabase PostgREST wrapper (not yet used)

**League Discovery:**
- `getAllLeagues_()` — Fetches all NFL leagues for authenticated user

**Standings:**
- `getLeagueStandings_(leagueKey)` — Fetches league standings with W-L-T, PF, PA

**Weekly Data:**
- `getCurrentWeek_(leagueKey)` — Determines current NFL week
- `getWeekMatchups_(week)` — Fetches scoreboard, calculates close matchups
- `getWeekTeamHighlights_(week, leagueKey)` — Top teams, blowouts, bad beats
- `getWeekBenchSummary_(week, leagueKey)` — Most points left on bench
- `getTopWaiverPickupsForWeek_(week, limit, leagueKey)` — Best pickups started

**Player Data:**
- `getWeekPointsMapForPlayerKeys_(week, playerKeys, leagueKey)` — Batch fetch player points
- `getWeekStartedPlayerKeys_(week, leagueKey)` — All non-bench players
- `getPlayerOwnerMap_(leagueKey)` — Maps player_key → team_name
- `getTopPlayersForWeekAndPosition_(week, position, limit, ownerMap, leagueKey)` — Position leaders

**Snapshot Assembly:**
- `buildLeagueSnapshot_(league)` — Assembles full weekly snapshot text
```

**Why:** A function index dramatically improves code navigation and onboarding speed.

---

### No Mechanism to Skip Leagues on Error
**File:** `Code.gs`
**Lines:** 1165-1173
**Risk:** If one league's API call fails, the entire script stops. Multi-league users get no snapshot at all.

**Current Code:**
```javascript
for (var i = 0; i < leagues.length; i++) {
  var snapshot = buildLeagueSnapshot_(leagues[i]);
  sections.push(snapshot);
  // If buildLeagueSnapshot_ throws, loop stops
}
```

**Recommended Fix:**
```javascript
for (var i = 0; i < leagues.length; i++) {
  try {
    var snapshot = buildLeagueSnapshot_(leagues[i]);
    sections.push(snapshot);

    if (completedWeek === null) {
      var cw = getCurrentWeek_(leagues[i].league_key);
      completedWeek = Math.max(1, cw - 1);
    }
  } catch (err) {
    Logger.log('Failed to build snapshot for league ' + leagues[i].name + ': ' + err.message);
    sections.push('==== ' + leagues[i].name + ' (' + leagues[i].league_key + ') ====\n\nError: ' + err.message);
  }
}
```

**Why:** Partial success is better than total failure. Users should get data for working leagues even if one fails.

---

### Email Quota Not Monitored
**File:** `Code.gs`
**Lines:** 1184
**Risk:** Apps Script has a daily email quota (100 emails/day for free accounts). No check means silent failure after quota exhaustion.

**Recommended Fix:**
```javascript
function sendSnapshotEmail_(recipientEmail, subject, body) {
  var quotaRemaining = MailApp.getRemainingDailyQuota();

  if (quotaRemaining <= 0) {
    Logger.log('Email quota exhausted. Cannot send snapshot.');
    throw new Error('Email quota exhausted (' + quotaRemaining + ' remaining). Snapshot not sent.');
  }

  Logger.log('Sending email. Remaining daily quota: ' + quotaRemaining);
  MailApp.sendEmail(recipientEmail, subject, body);
}
```

**Why:** Explicit quota checks provide clear error messages instead of silent failures.

---

## 🟡 Medium Priority (Next Sprint)

### Inconsistent Error Message Format
**File:** `Code.gs`
**Lines:** 14, 38, 47, 57, 139, 213, 226, 500, 649, 821
**Risk:** Error messages have inconsistent format, making them hard to grep in logs or monitor with alerts.

**Current Examples:**
```javascript
throw new Error('Supabase configuration missing. Check SUPABASE_URL and SUPABASE_ANON_KEY script properties.');
throw new Error('Supabase request failed. Status ' + status + ' Body: ' + body);
throw new Error('Missing YAHOO_LEAGUE_KEY script property.');
throw new Error('Unexpected users/games/leagues response structure from Yahoo.');
```

**Recommended Standard:**
```javascript
// Format: [FunctionName] Message: details
throw new Error('[getConfig_] Supabase configuration missing. Check SUPABASE_URL and SUPABASE_ANON_KEY script properties.');
throw new Error('[supabaseRequest_] HTTP ' + status + ': ' + body);
throw new Error('[getLeagueKey_] Missing YAHOO_LEAGUE_KEY script property.');
throw new Error('[getAllLeagues_] Unexpected response structure: missing users[0].user');
```

**Why:** Structured error messages enable better log analysis, monitoring, and debugging.

---

### No Documentation of Yahoo API Response Structure
**File:** `Code.gs`
**Lines:** 54-124, 138-204
**Risk:** The Yahoo API returns deeply nested, non-intuitive structures. Without documentation, future developers must reverse-engineer the parsing logic.

**Recommended Fix:**
Add inline comments with example response shapes:
```javascript
function getAllLeagues_() {
  /*
   * Yahoo API structure:
   * {
   *   fantasy_content: {
   *     users: {
   *       "0": {
   *         user: [
   *           { ... user metadata ... },
   *           {
   *             games: {
   *               count: 1,
   *               "0": {
   *                 game: [
   *                   { ... game metadata ... },
   *                   {
   *                     leagues: {
   *                       count: 2,
   *                       "0": { league: [ {...}, {...} ] },
   *                       "1": { league: [ {...}, {...} ] }
   *                     }
   *                   }
   *                 ]
   *               }
   *             }
   *           }
   *         ]
   *       }
   *     }
   *   }
   * }
   */
  var data = yahooApiRequest_('/users;use_login=1/games;game_keys=nfl/leagues', {});
  // ...
}
```

**Why:** API structure documentation prevents confusion and accelerates debugging.

---

### Magic Numbers Not Extracted as Constants
**File:** `Code.gs`
**Lines:** 435, 729, 903, 1009
**Risk:** Values like `chunkSize = 25` and `windowSec = 7 * 24 * 60 * 60` are repeated and lack context.

**Current Code:**
```javascript
var chunkSize = 25; // repeated in 2 functions
var windowSec = 7 * 24 * 60 * 60; // meaning unclear
```

**Recommended Fix:**
```javascript
// Add at top of file
var YAHOO_API_BATCH_SIZE = 25; // Yahoo API player batch limit
var WAIVER_PICKUP_WINDOW_DAYS = 7; // How far back to look for pickups
var WAIVER_PICKUP_WINDOW_SEC = WAIVER_PICKUP_WINDOW_DAYS * 24 * 60 * 60;
var MIN_WEEK = 1;
var MAX_WEEK = 18; // NFL regular season length
var TOKEN_REFRESH_BUFFER_SEC = 5 * 60; // Refresh token 5 min before expiry
```

**Why:** Named constants improve readability, prevent typos, and centralize configuration.

---

### No Unit Tests
**File:** N/A
**Risk:** All logic is untested. Refactoring or changes require manual end-to-end testing via Yahoo API (slow, brittle).

**Recommended Fix:**
While Apps Script doesn't have native testing, you can:
1. Extract pure logic into testable functions
2. Use [GasT framework](https://github.com/huan/gast)
3. Write integration tests that mock `UrlFetchApp`

**Why:** Tests enable confident refactoring and catch regressions early.

---

### Supabase Integration Incomplete
**File:** `Code.gs`
**Lines:** 11-42
**Risk:** `supabaseRequest_()` is implemented but never called. This suggests planned persistence that was abandoned, leaving dead code.

**Recommended Action:**
1. If Supabase persistence is planned: document the schema and integration roadmap in CLAUDE.md
2. If Supabase is abandoned: remove `supabaseRequest_()` and related config

**Why:** Dead code creates confusion and maintenance burden.

---

### No Caching of Expensive Lookups
**File:** `Code.gs`
**Lines:** 815-884, 494-641
**Risk:** `getPlayerOwnerMap_()` is called once per snapshot and fetches rosters for all teams. This is expensive but the result is reused multiple times.

**Current Code:**
```javascript
function buildLeagueSnapshot_(league) {
  // ...
  var ownerMap = getPlayerOwnerMap_(leagueKey); // fetches all rosters

  // ownerMap is used multiple times below
  var qbTop = getTopPlayersForWeekAndPosition_(completedWeek, 'QB', 3, ownerMap, leagueKey);
  var wrTop = getTopPlayersForWeekAndPosition_(completedWeek, 'WR', 3, ownerMap, leagueKey);
  // etc.
}
```

**Recommended Fix:**
Use CacheService to store owner map for 10 minutes:
```javascript
function getPlayerOwnerMap_(leagueKey) {
  leagueKey = leagueKey || getLeagueKey_();

  var cache = CacheService.getScriptCache();
  var cacheKey = 'ownerMap_' + leagueKey;
  var cached = cache.get(cacheKey);

  if (cached) {
    Logger.log('Using cached owner map for ' + leagueKey);
    return JSON.parse(cached);
  }

  var data = yahooApiRequest_('/league/' + leagueKey + '/teams;out=roster', {});
  // ... existing parsing logic ...

  cache.put(cacheKey, JSON.stringify(ownerMap), 600); // 10 min TTL
  return ownerMap;
}
```

**Why:** Caching reduces API calls and improves performance, especially for multi-league runs.

---

### Snapshot Output Is Plain Text
**File:** `Code.gs`
**Lines:** 1034-1152
**Risk:** Plain text emails are hard to read and don't support rich formatting (bold, tables, links).

**Recommended Fix:**
Generate HTML instead:
```javascript
function buildLeagueSnapshot_(league) {
  // ... existing logic ...

  var html = [];
  html.push('<h2>' + leagueName + ' (' + leagueKey + ')</h2>');
  html.push('<h3>LEAGUE STANDINGS</h3>');
  html.push('<table border="1" cellpadding="5" cellspacing="0">');
  html.push('<tr><th>Rank</th><th>Team</th><th>Manager</th><th>Record</th><th>PF</th><th>PA</th></tr>');

  rows.forEach(function(r) {
    var star = r.clinched_playoffs ? ' ⭐' : '';
    html.push('<tr>');
    html.push('<td>' + r.rank + '</td>');
    html.push('<td>' + r.team_name + star + '</td>');
    html.push('<td>' + (r.manager_name || '') + '</td>');
    html.push('<td>' + r.wins + '-' + r.losses + '-' + r.ties + '</td>');
    html.push('<td>' + Math.round(r.points_for || 0) + '</td>');
    html.push('<td>' + Math.round(r.points_against || 0) + '</td>');
    html.push('</tr>');
  });

  html.push('</table>');
  // ... rest of snapshot

  return html.join('');
}

function pullFantasyData() {
  // ...
  var htmlOutput = '<html><body>' + sections.join('<hr>') + '</body></html>';

  MailApp.sendEmail({
    to: recipientEmail,
    subject: subject,
    htmlBody: htmlOutput
  });
}
```

**Why:** HTML emails are more readable and professional. Tables especially improve standings presentation.

---

### No Deployment Checklist
**File:** `CLAUDE.md`
**Lines:** N/A
**Risk:** The deployment section lacks a step-by-step checklist. Easy to miss setting a required property or trigger.

**Recommended Fix:**
Add to CLAUDE.md:
```markdown
## Deployment Checklist

- [ ] Create Apps Script project
- [ ] Set all Script Properties (see table above)
- [ ] Deploy as Web App (Execute as: Me, Access: Anyone)
- [ ] Copy Web App URL to `YAHOO_REDIRECT_URI` property
- [ ] Run `startYahooAuth()` in IDE and complete OAuth flow
- [ ] Test `pullFantasyData()` manually
- [ ] Set up time-driven trigger (recommended: weekly, Monday 9 AM)
- [ ] Verify first automated run succeeds
- [ ] (Optional) Set up error notification forwarding
```

**Why:** Checklists prevent deployment mistakes and improve onboarding.

---

### README.md Is Minimal
**File:** `README.md`
**Lines:** 1-2
**Risk:** The README provides no value. Users landing on the GitHub repo have no idea what this project does.

**Recommended Fix:**
```markdown
# fantasy-snapshot

🏈 Automated Yahoo Fantasy Football weekly snapshot generator

Pulls your league standings, top performers, waiver pickups, and more — then emails you a comprehensive weekly report.

## Features

- League standings with playoff clinch indicators
- Top 3 highest scoring teams
- Biggest blowouts and "bad beat" of the week
- Most points left on the bench
- Top waiver pickups that were started
- Position leaders (QB, RB, WR, TE, K, DEF)

## Tech Stack

- Google Apps Script (V8 runtime)
- Yahoo Fantasy Sports API v2
- Supabase (planned for future data persistence)

## Setup

See [CLAUDE.md](CLAUDE.md) for detailed setup instructions.

## License

MIT
```

**Why:** A good README improves discoverability and helps users understand the project at a glance.

---

### No Logging of Snapshot Generation Time
**File:** `Code.gs`
**Lines:** 1155-1185
**Risk:** No visibility into how long snapshot generation takes. Slow runs can't be diagnosed.

**Recommended Fix:**
```javascript
function pullFantasyData() {
  var startTime = Date.now();
  try {
    // ... existing logic ...

    var endTime = Date.now();
    var durationSec = ((endTime - startTime) / 1000).toFixed(2);
    Logger.log('Snapshot generation completed in ' + durationSec + 's. Total API calls: ' + API_CALL_COUNT);

  } catch (err) {
    var endTime = Date.now();
    var durationSec = ((endTime - startTime) / 1000).toFixed(2);
    Logger.log('Snapshot generation failed after ' + durationSec + 's and ' + API_CALL_COUNT + ' API calls: ' + err.message);
    // ...
  }
}
```

**Why:** Duration logging helps identify performance issues and track improvements.

---

### No Way to Test Snapshot Without Sending Email
**File:** `Code.gs`
**Lines:** 1184
**Risk:** Testing requires sending real emails, which is slow and pollutes the inbox.

**Recommended Fix:**
Add a debug function:
```javascript
function debugSnapshotToLog() {
  // Same as pullFantasyData() but logs instead of emailing
  try {
    var leagues = getAllLeagues_();
    if (!leagues || !leagues.length) {
      Logger.log('No leagues found for this Yahoo account.');
      return;
    }

    var sections = [];
    var completedWeek = null;

    for (var i = 0; i < leagues.length; i++) {
      var snapshot = buildLeagueSnapshot_(leagues[i]);
      sections.push(snapshot);

      if (completedWeek === null) {
        var cw = getCurrentWeek_(leagues[i].league_key);
        completedWeek = Math.max(1, cw - 1);
      }
    }

    var output = sections.join('\n\n');
    Logger.log('=== SNAPSHOT OUTPUT ===\n' + output);
    Logger.log('Total API calls: ' + API_CALL_COUNT);

  } catch (err) {
    Logger.log('Error generating snapshot: ' + err.message);
    Logger.log('Stack trace: ' + err.stack);
  }
}
```

**Why:** Faster development iteration and easier debugging without email spam.

---

### Week Range Not Validated
**File:** `Code.gs`
**Lines:** 1069
**Risk:** `completedWeek = Math.max(1, currentWeek - 1)` can produce week 1 even during preseason (week 0), causing confusing snapshots.

**Recommended Fix:**
```javascript
var currentWeek = getCurrentWeek_(leagueKey);
if (currentWeek < 2) {
  Logger.log('Season has not yet started (current week: ' + currentWeek + '). No completed week to snapshot.');
  return 'Season has not yet started for ' + leagueName + '. Check back after Week 1.';
}
var completedWeek = currentWeek - 1;
```

**Why:** Gracefully handle preseason and early season edge cases.

---

### No Analytics or Usage Tracking
**File:** N/A
**Risk:** No visibility into how often the script runs, which leagues are tracked, or what errors occur in production.

**Recommended Fix:**
If Supabase integration is completed, log telemetry:
```javascript
function logSnapshotEvent_(eventType, metadata) {
  try {
    var payload = {
      event_type: eventType,
      metadata: metadata,
      timestamp: new Date().toISOString()
    };

    supabaseRequest_('/rest/v1/snapshot_events', 'POST', payload);
  } catch (err) {
    Logger.log('Failed to log event: ' + err.message);
    // Don't throw - telemetry failures should not break main flow
  }
}

function pullFantasyData() {
  var startTime = Date.now();
  try {
    // ... existing logic ...

    logSnapshotEvent_('snapshot_success', {
      league_count: leagues.length,
      duration_ms: Date.now() - startTime,
      api_call_count: API_CALL_COUNT
    });

  } catch (err) {
    logSnapshotEvent_('snapshot_error', {
      error_message: err.message,
      duration_ms: Date.now() - startTime
    });
    // ...
  }
}
```

**Why:** Analytics enable product improvements and operational insights.

---

## ⚪ Low Priority (Backlog)

### Function Names Use Trailing Underscore Convention
**File:** `Code.gs`
**Lines:** All private functions
**Risk:** Trailing underscore (`_`) for private functions is unconventional in JavaScript. Leading underscore (`_functionName`) is more common.

**Recommended Action:**
Rename `getConfig_()` → `_getConfig()` (if desired). However, this is a stylistic choice with low impact.

**Why:** Aligning with common JavaScript conventions improves readability for new contributors.

---

### No TypeScript Definitions
**File:** N/A
**Risk:** No type safety or IDE autocomplete for Apps Script code.

**Recommended Action:**
If the project grows, consider using [@types/google-apps-script](https://www.npmjs.com/package/@types/google-apps-script) with clasp and TypeScript.

**Why:** Type safety catches bugs at compile time and improves developer experience.

---

### Supabase Anon Key Used for Server-Side Calls
**File:** `Code.gs`
**Lines:** 21-22
**Risk:** Using `SUPABASE_ANON_KEY` on the server side is unconventional. Typically, server-side code uses a service role key for elevated privileges.

**Recommended Action:**
If Supabase persistence is enabled:
1. Add `SUPABASE_SERVICE_ROLE_KEY` script property
2. Use service role key in `supabaseRequest_()` for server operations
3. Reserve anon key for client-side (if applicable)

**Why:** Service role keys have full access and bypass RLS policies, which is appropriate for server-side batch operations.

---

### No Performance Benchmarking
**File:** N/A
**Risk:** No baseline performance metrics. Can't measure impact of optimizations.

**Recommended Action:**
After implementing duration logging, establish baseline:
- Single league: target <10s
- Multi-league: target <30s
- API call count: target <50 per league

**Why:** Benchmarks enable data-driven optimization.

---

### No Email Template Customization
**File:** `Code.gs`
**Lines:** 1034-1152
**Risk:** Snapshot format is hardcoded. Users can't customize which sections to include or exclude.

**Recommended Action:**
Add optional config:
```javascript
var SNAPSHOT_CONFIG = {
  includeStandings: true,
  includeTopTeams: true,
  includeBlowouts: true,
  includeBadBeat: true,
  includeBenchPoints: true,
  includeWaiverPickups: true,
  includePositionLeaders: true,
  positions: ['QB', 'WR', 'RB', 'TE', 'K', 'DEF']
};
```

**Why:** Customization improves user satisfaction, but is low priority until requested.

---

### No CI/CD Pipeline
**File:** N/A
**Risk:** Deployments are manual via clasp. No automated testing or linting on commit.

**Recommended Action:**
Set up GitHub Actions to:
1. Lint code with ESLint
2. Run tests (if added)
3. Auto-deploy to staging environment on PR merge

**Why:** CI/CD improves code quality and deployment confidence, but is overkill for a single-file script.

---

### No Rollback Strategy
**File:** N/A
**Risk:** If a bad deployment breaks the script, there's no documented rollback process.

**Recommended Action:**
Document rollback in CLAUDE.md:
```markdown
## Rollback Procedure

1. Find previous working version ID in Apps Script version history
2. Restore that version: File → Manage versions → Restore
3. Alternatively, use `clasp versions` and `clasp deploy -v <version>`
```

**Why:** Clear rollback procedures reduce downtime, but are low priority for non-critical apps.

---

## 📋 Action Checklist

### Critical (Fix Immediately)
- [ ] Code.gs:1184 - Extract hardcoded email to `RECIPIENT_EMAIL` script property
- [ ] Code.gs:1155-1185 - Add try-catch to `pullFantasyData()` with error notification email

### High Priority (This Week)
- [ ] Code.gs:436,731 - Add `Utilities.sleep(200)` between batched API calls for rate limiting
- [ ] Code.gs:1298-1313,33 - Implement `retryWithBackoff_()` and wrap `UrlFetchApp.fetch()`
- [ ] Code.gs:428,721,887,494,643,815 - Add input validation for `week` parameters (1-18 range)
- [ ] Code.gs:1273-1280 - Proactively check token expiration in `getYahooAccessToken_()`
- [ ] Code.gs:all - Add `API_CALL_COUNT` global counter and log in `pullFantasyData()`
- [ ] CLAUDE.md - Add function index section for quick navigation
- [ ] Code.gs:1165-1173 - Wrap `buildLeagueSnapshot_()` in try-catch to handle league-specific failures
- [ ] Code.gs:1184 - Add `MailApp.getRemainingDailyQuota()` check before sending email

### Medium Priority (Next Sprint)
- [ ] Code.gs:all - Standardize error message format with `[FunctionName]` prefix
- [ ] Code.gs:54-124,138-204 - Add inline comments documenting Yahoo API response structure
- [ ] Code.gs:top - Extract magic numbers to named constants (batch size, week range, etc.)
- [ ] Project - Investigate unit testing with GasT framework
- [ ] Code.gs:11-42 - Decide on Supabase integration: complete or remove dead code
- [ ] Code.gs:815-884,494-641 - Add CacheService caching for `getPlayerOwnerMap_()`
- [ ] Code.gs:1034-1152 - Convert snapshot output from plain text to HTML
- [ ] CLAUDE.md - Add deployment checklist section
- [ ] README.md - Expand with project description, features, and setup link
- [ ] Code.gs:1155-1185 - Add duration logging to `pullFantasyData()`
- [ ] Code.gs - Add `debugSnapshotToLog()` function for testing without email
- [ ] Code.gs:1069 - Validate `currentWeek >= 2` before generating snapshot

### Low Priority (Backlog)
- [ ] Code.gs:all - Consider renaming private functions from `func_()` to `_func()`
- [ ] Project - Evaluate TypeScript with @types/google-apps-script
- [ ] Code.gs:21-22 - Replace `SUPABASE_ANON_KEY` with `SUPABASE_SERVICE_ROLE_KEY`
- [ ] Project - Establish performance benchmarks after adding duration logging
- [ ] Code.gs:1034-1152 - Add optional config for customizing snapshot sections
- [ ] Project - Set up GitHub Actions CI/CD pipeline
- [ ] CLAUDE.md - Document rollback procedure

---

## 📊 Trends Since Last Audit

**Initial audit** - No previous audit for comparison.

---

## 🎯 Recommendations for Next Audit

Focus areas for next week:
1. **Security**: Verify all secrets are in Script Properties (no hardcoded values)
2. **Error handling**: Confirm try-catch blocks are in place for all external calls
3. **Performance**: Review API call count logs and caching effectiveness
4. **Testing**: Check if any unit tests have been added
5. **Documentation**: Verify CLAUDE.md updates are complete and accurate
